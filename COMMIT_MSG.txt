Add safety checks and validation command

Safety features:
- Python 3.11+ version check at startup
- .gitignore verification in install command
- Auto-add .env to .gitignore if missing
- validate command tests all configuration paths

Features:
- ADD cmd_validate() - comprehensive path validation
  - Checks game installation + templates directory
  - Checks saves directory + counts savegames
  - Checks KoboldCpp executable + model file
  - Verifies .gitignore protection for .env
  - Clear pass/fail/warning reporting

Changes:
- terractl.py: +130 lines (version check + validate + gitignore)
- Python 3.11+ enforced (tomllib requirement)
- install command now ensures .gitignore safety
- validate command catches config errors before build
- README.md: document validate command

Usage:
  python terractl.py install   # Auto-adds .env to .gitignore
  python terractl.py validate  # Test config before build
